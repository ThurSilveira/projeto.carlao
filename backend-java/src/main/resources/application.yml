server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/escala_ministerial
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
